---
- name: create logical volume on prod
  hosts: prod
  become: yes
  tasks:
    - name: create partition
      community.general.parted:
        device: /dev/sdb
        number: 1
        state: present
